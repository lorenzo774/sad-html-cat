export class Audio {
  static jump = document.querySelector(
    "#jump"
  ) as HTMLAudioElement;

  static run = document.querySelector(
    "#run"
  ) as HTMLAudioElement;

  static star = document.querySelector(
    "#star  "
  ) as HTMLAudioElement;
}
